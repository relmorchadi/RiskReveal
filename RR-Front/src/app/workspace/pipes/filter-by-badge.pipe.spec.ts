import { FilterByBadgePipe } from './filter-by-badge.pipe';

describe('FilterByBadgePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByBadgePipe();
    expect(pipe).toBeTruthy();
  });
});
