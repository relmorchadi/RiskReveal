import { SystemTagFilterPipe } from './system-tag-filter.pipe';

describe('SystemTagFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SystemTagFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
