import { FilterBySystemTagPipe } from './filter-by-system-tag.pipe';

describe('FilterBySystemTagPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySystemTagPipe();
    expect(pipe).toBeTruthy();
  });
});
