import { FilterByUserTagPipe } from './filter-by-user-tag.pipe';

describe('FilterByUserTagPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByUserTagPipe();
    expect(pipe).toBeTruthy();
  });
});
