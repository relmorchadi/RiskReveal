import { FilterByPathPipe } from './filter-by-path.pipe';

describe('FilterByPathPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByPathPipe();
    expect(pipe).toBeTruthy();
  });
});
