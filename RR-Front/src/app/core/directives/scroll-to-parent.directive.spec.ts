import {ScrollToParentDirective} from './scroll-to-parent.directive';

describe('ScrollToParentDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToParentDirective();
    expect(directive).toBeTruthy();
  });
});
