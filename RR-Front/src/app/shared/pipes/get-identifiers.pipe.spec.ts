import { GetIdentifiersPipe } from './get-identifiers.pipe';

describe('GetIdentifiersPipe', () => {
  it('create an instance', () => {
    const pipe = new GetIdentifiersPipe();
    expect(pipe).toBeTruthy();
  });
});
