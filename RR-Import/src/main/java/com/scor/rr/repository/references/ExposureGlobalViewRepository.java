package com.scor.rr.repository.references;

import com.scor.rr.domain.entities.references.ExposureGlobalView;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ExposureGlobalViewRepository extends JpaRepository<ExposureGlobalView, Integer> {
}
